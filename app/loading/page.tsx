'use client';
import { Loading } from '@/components/about/loading';
import React from 'react';

const LoadingPage = () => {
    return (
        <>
            <Loading />
        </>
    );
};

export default LoadingPage;
